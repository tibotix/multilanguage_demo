This is an example Project to demonstrate multiple language bindings for a single C++ library.


Branches:
- master
- release
- base-lib
- python-bindings
- go-bindings
- c-bindings
  
Use `pre-commit install` to install the pre-commit hooks